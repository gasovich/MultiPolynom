Многочлен от многих переменных описывается таблицей в формате .CSV с разделителем ";",
где в первой строке записаны имена переменных,  во второй строке - значения переменных,
первый столбец содержит коэффициенты при одночленах,
в строках записаны степени переменных. Файл можно создать в программе Calc или вручную.

Сделано:
	1. Функция чтения в массив строки имен переменных
	2. Функция чтения коэффициентов и степеней в стркутуру, описывющую многочлен
	3. Дополнить класс многочлена вектором значений переменных и функцией вычисления значения многочлена	
	4. Обернуть структуру и функции по работе с многочленами в класс

Сделать
	- Применить vector из библиотеки STL
	- Поправить функции чтения так, чтобы при пустом списке имен переменных имена создавались по шаблону x1, x2, ... xN
	- Красивый вывод на экран многочлена и/или вывод его в формате Latex
	- Функция приведения подобных членов
	- Класс одночленов с арифметикой
	- Сложение и вычитание многочленов. Применить шаблоны
	- Умножение многочлена на одночлен
	- Перемножение многочленов
	- Лексикографическое упорядочивание
